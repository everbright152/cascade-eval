# Per-language wordlists for the dictionary-hit proxy.
# One token per line, lowercase. File name = language code, e.g. eng.txt, ara.txt.
# Absent file = no dictionary for that language -> proxy reports None (honest gap),
# never a fabricated score. Low-resource langs (Coptic/Jawi) are expected to have none.
