# Optional: enables the vision-LLM fallback engine for low-resource /
# handwritten pages. The harness runs fine WITHOUT this — it will skip the
# fallback engine and log its absence as a routing reason.
#
# Copy to .env and fill in to enable:
#   cp .env.example .env
ANTHROPIC_API_KEY=
